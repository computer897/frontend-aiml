# API Configuration
# Set this to your FastAPI backend URL
VITE_API_URL=http://localhost:8000

# Socket.IO signaling URL (Node.js signaling server, separate from FastAPI API)
# Use http(s) URL, not ws/wss.
VITE_SOCKET_URL=http://localhost:5000
